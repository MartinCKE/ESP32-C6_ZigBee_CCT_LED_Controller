# folder workspace

This workspace contains a minimal scaffold for development.

Structure
- `src/` — source files (empty)

Usage
- Open this folder in VS Code.
- Create a virtual environment for Python work: `python -m venv .venv` and activate before installing dependencies.

Notes
- Replace this README with project-specific details.
# ESP32-C6_ZigBee_CCT_LED_Controller
